# Machine-local Duaer state — not meant to be shared.

# Local pointer to the current feature directory. Rewritten when you switch features.
feature.json

# Written by `duaer init` in adopting projects (optional to commit there).
duaer-init.json

# Per-machine extension config overrides.
extensions/*/local-config.yml
