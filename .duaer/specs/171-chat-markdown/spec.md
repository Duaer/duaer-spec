# 171 — Render Markdown in chat bubbles

## Goal

Live-desk chat shows inline Markdown (e.g. **找/筛**) as formatted text, not raw markers.

## Acceptance

1. `**bold**`, inline code, http(s) links render in bubbles
2. Raw HTML is escaped
3. Streaming finishes with rendered HTML
