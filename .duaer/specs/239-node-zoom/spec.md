# 239 — Restore node click zoom on the dispatch stage

## Goal

Clicking a node on the dispatch-center battlefield must enlarge that node
again. Keep the graph on the page; do not open a second present window.

## Acceptance

1. Stage mounts enable Archify reveal zoom (`maxScale` 2.6).
2. Desk architecture embeds still disable in-place zoom and open present.
3. Tests + E2E + CHANGELOG; merge develop; handoff 8787.
