# 144 — Architecture node passport offset

## Goal

In the desk architecture embed, shift the Archify `.focus-chip` popup
100px left and 100px up from its current top-left inset.

## Acceptance

1. Embed fit CSS uses `left`/`top` reduced by 100px vs prior 0.75rem inset
2. Unit tests still pass; live smoke passes
