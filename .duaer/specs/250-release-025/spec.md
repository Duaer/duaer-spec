# Feature Specification: Release 0.25.0

## Goal

Ship 0.25.0: today’s FDE desk features (bug path, timeline, quick nav, post-deploy
CTAs) plus pending develop desk work; promote develop → main; GitHub Release;
npm Trusted Publishing.

## Acceptance

1. `package.json` is `0.25.0`; CHANGELOG has `0.25.0` (Unreleased cleared).
2. README / README.zh-CN list the new desk capabilities.
3. Tag `v0.25.0` on main; GitHub Release published; npm publish workflow runs.
