# Tasks

- [x] Stop `renderModuleTabs` inside `syncConfirmEnabled`
- [x] Tab switch → validate; after confirm → next draft module
- [x] Fix `setBusy` / restore `locked` alignment + hints
- [x] i18n + E2E + tests; stamp delivery
