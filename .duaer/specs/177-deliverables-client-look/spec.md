# Fix: client-facing deliverables page look

## Goal

The generated deliverables HTML (for customers) uses a spacious white
document look — not the FDE desk dark/orange palette.

## Acceptance

1. Page background is white / near-white; body text is dark
2. No desk tokens (steel plate, register orange, lock teal as primary chrome)
3. Layout reads grand: large title, clear stage hierarchy, roomy spacing
4. Existing content/structure and XSS escaping unchanged; unit + smoke still pass
