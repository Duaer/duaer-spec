# Baseline (project-specific)

This file is a placeholder.

In MyDesk, the real baseline lives at `docs/spec/00-baseline.md`.
When you adopt these standards in another project, replace this with that
project's frozen decisions (language, stack, architecture boundaries).
