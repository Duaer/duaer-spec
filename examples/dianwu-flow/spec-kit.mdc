---
description: 功能与热修都必须走 Spec Kit；禁止口头需求直接改代码
alwaysApply: true
---

# Spec Kit 必走（NON-NEGOTIABLE）

约定见仓库根目录 `SPECKIT.md` 与 `.specify/memory/constitution.md`。

## 何时必须走 Spec Kit

**一律先 Spec，再写代码。** 包括：

- 新功能 / 用户可见行为变更
- 编排、Catalog Brain、验收、缺口写码、Studio 时间线、分享页等**核心路径**
- 架构或检索/元数据契约变更
- **热修 / 回归 / 文案 / 单测门禁相关修复**（不可再以「热修」为由跳过 Spec）

禁止：口头需求或聊天里直接开改。

## 开干前 MUST 按序阅读

1. `.specify/memory/constitution.md`
2. `.specify/memory/project-context.md`（as-is 全景）
3. `CORE.md`
4. 含 UI / 体验：`.specify/memory/design-system.md`，并用 Mobbin MCP 对照
5. **用户可见改动**：遵守 `.cursor/rules/i18n.mdc`（`zh-CN`/`en` 同步）
6. **新 Agent / 新配方**：遵守 `.cursor/rules/agent-quality-gates.mdc`（夹具 + 整链验收）

## 流程

**默认（功能 / 架构）：**  
`/speckit-specify` → `/speckit-plan` → `/speckit-tasks` → `/speckit-implement` → `/speckit-converge`

**热修（仍须 Spec，可缩短）：**  
`/speckit-specify`（标明 hotfix：现象 / 根因 / 验收）→ `/speckit-tasks`（可极少任务）→ `/speckit-implement` → `/speckit-converge`  
plan 可与 specify 合并进同一 feature 目录的短 `plan.md`，**不可省略 specify 与 converge**。

- 有 `tasks.md` 时**只按任务实现**
- **Git**：功能从 `develop` 开 `feat/*`，先合 develop 再合 main；热修从 `main` 开 `fix/*`，先合 develop 再上正式（`.cursor/rules/git-flow.mdc`）
- 改架构 → 同步更新 `project-context.md`
- 改视觉 → 同步更新 `design-system.md`
- **改用户可见文案 / 新 UI** → 同步 `messages/zh-CN.json` + `en.json`（见 i18n 规则）
- 合并前按 `.specify/memory/testing.md` 跑相关门禁；**单测之后必须用真实账号浏览器实测**，再合 develop / main（热修可缩测试层，但须在 spec/tasks 写明测什么，账号实测不能省用户可见路径）

## 冲突优先级

代码 ≈ `project-context` > `design-system` > `CORE` > Constitution > `EVOLUTION`

## 用词

对外与注释说 **Agent**，不说「积木注册表」。
