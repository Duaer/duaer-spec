# DianWu Flow overlay (example only)

This directory is a **sample product overlay**, not the Duaer method itself.

When adopting [duaer-spec](https://github.com/fujiezee/duaer-spec), prefer the
repository root:

- [`DUADER.md`](../../DUADER.md)
- [`.duaer/`](../../.duaer/)
- [`AGENTS.md`](../../AGENTS.md)

Use this folder only as inspiration for product-specific memory (constitution,
testing, git policy). Conflicts with root agent ops → **root wins**.

## Historical notes

This overlay originally documented a product that used `develop` / `feat/*` /
`fix/*` and bilingual UI rules. Those policies apply only if you deliberately
copy them into a target product — never into duaer-spec itself.
